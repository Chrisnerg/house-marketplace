services:
  postgres:
    image: postgres:16
    container_name: house_market_place
    restart: unless-stopped
    ports:
      - "5436:5432"
    environment:
      POSTGRES_DB: house_market_place
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ChrisDev
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
